document.addEventListener('DOMContentLoaded', () => {
  // Create a gameboard object
  const gameBoard = (function () {
    let board = ['', '', '', '', '', '', '', '', ''];

    return {board};
  })();
});
